using System;

namespace ForLoopPractice
{
    class Program
    {
        static void Main(string[] args)
        {
            var studentNames = new string[] { "Michael", "Anyong", "Lucille" };
            foreach (var student in studentNames)
            {
                Console.WriteLine(student);
                Console.WriteLine(student);
            }

            var studentPeople = new string[] { "Bob Loblaw", "Barry", "Gene" };
            foreach (var student in studentPeople)
            {
                Console.WriteLine(student);
                Console.WriteLine();
            }

            var studentTypes = new string[] { "Buster", "Tobias", "Maebe" };
            foreach (var student in studentTypes)
            {
                Console.WriteLine(student);
                Console.WriteLine();

            }
            {
                Console.WriteLine();

                Console.WriteLine("You're doing it, goddammit!");

                    }
            var studentNames = new string[] { "Brett", "David", "Michael" };
            foreach (var student in studentNames)
            {
                Console.WriteLine(student);
            }

        }
    }
}

    

